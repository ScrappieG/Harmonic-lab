[project]
name = "articuleet-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "openai>=1.0.0",
  "python-multipart"
]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]