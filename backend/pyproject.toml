[project]
name = "articuleet-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "httpx",
  "python-multipart"
]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
