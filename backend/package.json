{
  "name": "articuleet",
  "private": true,
  "scripts": {
    "deploy": "cd backend && uv run pywrangler deploy",
    "deploy:prod": "cd backend && uv run pywrangler deploy --env production",
    "dev": "cd backend && uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}
