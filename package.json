{
  "name": "articuleet",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy --env=\"\"",
    "deploy:prod": "uv run pywrangler deploy --env production",
    "dev": "uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.67.0"
  }
}